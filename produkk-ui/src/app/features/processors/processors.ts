import { Component } from '@angular/core';
import { NavbarComponent } from '../../layout/navbar/navbar';

@Component({
  selector: 'app-processors',
  standalone: true,
  imports: [NavbarComponent],
  templateUrl: './processors.html',
})
export class ProcessorsComponent {}
